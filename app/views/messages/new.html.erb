<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-6 col-md-offset-3">
      <div class="centered-flexbox">
        <div class="message-response-form-container">
        <p>New message to <strong><%= @recipient.first_name %></strong> regarding booking with <strong><%= @booking.personality.first_name %></strong>
         <p>Booking on <%= @booking.date %></p></p>
        <%= simple_form_for [@booking, @message] do |f| %>
         <%= f.input :content %>
         <%= f.input :recipient_id, :as => :hidden, :input_html => { :value => @recipient.id } %>
         <%= f.submit "Send Message", :class => "btn btn-green"  %>
        <% end %>
      </div>
      </div>
    </div>
  </div>
</div>
